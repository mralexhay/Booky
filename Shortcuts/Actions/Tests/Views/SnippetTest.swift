//
//  SnippetTest.swift
//  Booky
//
//  Created by Alex Hay on 11/06/2022.
//

import SwiftUI

/*
struct SnippetTest: View {
     var body: some View {
         VStack {
             Link(destination: URL(string: "https://en.wikipedia.org/wiki/%22Hello,_World!%22_program")!, label: {
                 Image("helloworld")
                     .resizable()
                     .aspectRatio(contentMode: .fit)
                     .padding(.bottom)
             })
             
             Link(destination: URL(string: "shortcuts://run-shortcut?name=Snippet%20Test")!, label: {
                 Text("Run Shortcut")
             })
             .bold()
             .foregroundColor(.teal)
             .padding(.bottom)
             
             Link(destination: URL(string: "https://www.apple.com")!, label: {
                 Text("Apple")
                     .padding(.horizontal, 6)
                     .padding(.vertical, 3)
             })
             .foregroundColor(.white)
             .background {
                 Color.blue
                     .cornerRadius(3)
             }
             .padding(.bottom)
             
             Link(destination: URL(string: "https://www.macstories.net")!, label: {
                 Text("MacStories")
             })
             .bold()
             .foregroundColor(.red)
             .padding(.bottom)
             
             HStack {
                 Link(destination: URL(string: "bear://")!, label: {
                     Text("Bear")
                         .padding(.horizontal, 6)
                         .padding(.vertical, 3)
                 })
                 .foregroundColor(.white)
                 .background {
                     Color.red
                         .cornerRadius(3)
                 }
                 .padding(.trailing, 4)
                 Link(destination: URL(string: "drafts://")!, label: {
                     Text("Drafts")
                         .padding(.horizontal, 6)
                         .padding(.vertical, 3)
                 })
                 .foregroundColor(.white)
                 .background {
                     Color.secondary
                         .cornerRadius(3)
                 }
                 .padding(.leading, 4)
             }
             .padding(.bottom)
         }
         .padding()
     }
 }
 */
